import 'package:flutter/material.dart';

class HitScreen extends StatelessWidget {
  const HitScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("HitScreen"),
      ),
      body: Container(),
    );
  }
}
